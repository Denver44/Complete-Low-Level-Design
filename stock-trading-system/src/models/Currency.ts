/**
 * Currency Enum
 *
 * Defines the supported currencies for stock prices
 * Using enum provides type safety and prevents invalid currency values
 */
enum Currency {
  USD = "USD",  // US Dollar
  INR = "INR"   // Indian Rupee
}

export default Currency;
